langgraph
langchain
langchain-community
langchain-ollama
langchain-text-splitters
chromadb
langchain-chroma
python-dotenv

# LiveKit voice agent
livekit-agents[mcp]>=1.2.0
livekit-plugins-google>=1.0.0
livekit-plugins-silero>=1.0.0

# Web mic UI + token server (WSL-friendly browser microphone via LiveKit)
fastapi
uvicorn[standard]
jinja2

langchain-google-genai

# Persistent agent memory (PostgreSQL)
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# Level 1 voice pipeline (mic -> VAD -> faster-whisper)
numpy
sounddevice
torch
faster-whisper
